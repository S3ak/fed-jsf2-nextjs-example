import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // cacheComponents: true,
  // 1. Enable the experimental test proxy
  // experimental: {
  //   testProxy: true,
  // },
};

export default nextConfig;
